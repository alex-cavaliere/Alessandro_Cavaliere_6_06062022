function filterTitle(data, media){
    const mediaSection = document.querySelector(".photograph-body");
    const template = mediasFactory(data);
    arr.push(media._title);
    let filteredTitle = arr.sort()
    console.log(filteredTitle)
}